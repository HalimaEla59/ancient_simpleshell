#include "shell.h"
/**
 * get_cmd - Function to read command
 * Return: 0 on success
 */
char get_cmd(void)
{
  size_t n = 0;
  char *cmd = NULL;
  printf("$ ");
  read = getline(&cmd, &n, stdin);
  return (0);
}
